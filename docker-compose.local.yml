# Local development docker-compose
# Usage: docker-compose -f docker-compose.local.yml up --build
version: '3.8'

services:
  vimtractor:
    build: .
    container_name: vimtractor-local
    environment:
      - NODE_ENV=production
      - PORT=5110
    ports:
      - "5110:5110"
    volumes:
      - ./data:/app/data  # Use local data directory
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5110/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 5s
